# MediaClassifier GUI 设计文档

## 1. 概述

MediaClassifier GUI 是基于 Slint 框架构建的图形用户界面，为用户提供直观、易用的媒体文件分类体验。本文档描述了 GUI 的设计规范和实现细节。

### 1.1 技术选型

- **GUI 框架**：[Slint](https://slint.dev/) - 现代化、高性能的 Rust GUI 框架
- **设计语言**：Slint DSL (.slint 文件)
- **集成方式**：通过 `slint-build` 编译时生成 Rust 代码

### 1.2 设计目标

- 简洁直观的用户界面
- 与 CLI 功能完全对等
- 实时反馈处理进度
- 支持跨平台运行（Windows、macOS、Linux）
- 支持 i18n 国际化
- 支持主题切换（明/暗/自动）

## 2. 页面结构

应用包含两个主要页面：

### 2.1 主页面 (Main Page)

```
┌─────────────────────────────────────────────────────────────┐
│  🎬 MediaClassifier    [自动][☀️][🌙]    [⚙️ 配置]           │ 导航栏
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  工作目录                                                   │
│                                                             │
│  ┌─────────────────────────────────────────────────┐ ┌────┐│
│  │ 点击选择文件夹...                                │ │ 📁 ││ 目录选择
│  └─────────────────────────────────────────────────┘ └────┘│
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │              选择工作目录 / 开始工作                     ││ 操作按钮
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
│  (工作时显示)                                               │
│  处理进度                                           65%     │
│  [████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░]       │ 进度条
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                    显示详情                              ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
│  (展开详情时)                                               │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ [日志内容滚动区域]                                       ││
│  │ 正在处理: IMG_1234.jpg                                   ││ 日志区域
│  │ 移动到: Photos/2025/01/IMG_1234.jpg                      ││
│  │ ...                                                      ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 配置页面 (Config Page)

```
┌─────────────────────────────────────────────────────────────┐
│  🎬 MediaClassifier    [自动][☀️][🌙]    [🏠 主页]           │ 导航栏
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ⚙️ 配置管理                               [➕ 新增规则]     │
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 规则名称     │ 扩展名        │ 目录模板          │启用│  ││
│  │─────────────┼───────────────┼──────────────────┼────┼──││
│  │ 高质量照片   │ jpg,jpeg,png  │ Photos/{year}... │ ✓  │🗑️││
│  │ RAW照片      │ nef,cr2,arw   │ RAW/{year}/{mo...│ ✓  │🗑️││ 规则表格
│  │ 视频         │ mp4,mov,avi   │ Videos/{year}    │ ✓  │🗑️││
│  │ 音乐         │ mp3,flac,wav  │ Music/{ext}      │ ✗  │🗑️││
│  │ ...          │               │                  │    │  ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 统计结果弹窗

```
┌─────────────────────────────────────┐
│                                     │
│         📊 处理完成                  │
│  ─────────────────────────────────  │
│                                     │
│  总计:      100                     │
│  ✅ 成功:    85                     │
│  🔄 重命名:   3                     │
│  ⏭️ 跳过:    10                     │
│  ❌ 失败:     2                     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │           关闭               │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

### 2.4 新增规则弹窗

```
┌─────────────────────────────────────┐
│                                     │
│        ➕ 新增规则                   │
│  ─────────────────────────────────  │
│                                     │
│  规则名称:  [________________]      │
│  规则描述:  [________________]      │
│  扩展名:    [jpg,png,gif_____]      │
│  目录模板:  [{ext}/{date}_____]     │
│  最小大小:  [0B______________]      │
│  最大大小:  [无限制__________]      │
│  启用:      ☑                       │
│                                     │
│            [取消]    [保存]         │
│                                     │
└─────────────────────────────────────┘
```

## 3. 组件详细设计

### 3.1 导航栏

| 组件 | 说明 |
|------|------|
| 应用标题 | "🎬 MediaClassifier"，18px 粗体 |
| 主题切换 | 三个按钮：自动/浅色(☀️)/深色(🌙) |
| 页面切换 | 配置页按钮或主页按钮 |

### 3.2 主页面组件

| 组件 | 类型 | 说明 |
|------|------|------|
| 工作目录标题 | Text | "工作目录"，20px 粗体 |
| 目录输入框 | LineEdit | 显示选择的目录路径 |
| 浏览按钮 | Button | 📁 图标，打开目录选择对话框 |
| 开始按钮 | Button | 动态文本：未选择时"选择工作目录"，选择后"开始工作" |
| 进度指示器 | ProgressIndicator | 0-100% 进度条 |
| 详情按钮 | Button | 切换日志显示/隐藏 |
| 日志区域 | ScrollView + Text | 滚动显示日志内容 |

### 3.3 配置页面组件

| 组件 | 类型 | 说明 |
|------|------|------|
| 页面标题 | Text | "⚙️ 配置管理"，20px 粗体 |
| 新增按钮 | Button | "➕ 新增规则"，主要按钮样式 |
| 规则表格 | ScrollView + VerticalBox | 显示所有规则 |
| 删除按钮 | Button | 🗑️ 图标，删除对应规则 |

## 4. 数据结构

### 4.1 RuleItem (分类规则)

```slint
export struct RuleItem {
    id: int,                    // 规则ID
    name: string,               // 规则名称
    description: string,        // 规则描述
    extensions: string,         // 支持的扩展名（逗号分隔）
    directory_template: string, // 目录模板
    min_size: string,           // 最小文件大小
    max_size: string,           // 最大文件大小
    enabled: bool,              // 是否启用
}
```

### 4.2 Statistics (统计信息)

```slint
export struct Statistics {
    total: int,    // 总文件数
    success: int,  // 成功数
    skipped: int,  // 跳过数
    renamed: int,  // 重命名数
    failed: int,   // 失败数
}
```

### 4.3 AppState (应用状态)

```slint
export enum AppState {
    Idle,       // 空闲（等待用户操作）
    Working,    // 工作中（处理文件）
    Completed,  // 完成
}
```

### 4.4 PageType (页面类型)

```slint
export enum PageType {
    Main,    // 主页面
    Config,  // 配置页面
}
```

### 4.5 ThemeMode (主题模式)

```slint
export enum ThemeMode {
    Auto,   // 自动跟随系统
    Light,  // 浅色模式
    Dark,   // 深色模式
}
```

### 4.6 I18nStrings (国际化字符串)

```slint
export struct I18nStrings {
    // 通用
    app_title: string,
    // 主页面
    working_directory: string,
    select_directory: string,
    start_working: string,
    show_details: string,
    hide_details: string,
    progress_label: string,
    // 统计弹窗
    stats_title: string,
    stats_total: string,
    stats_success: string,
    stats_renamed: string,
    stats_skipped: string,
    stats_failed: string,
    stats_close: string,
    // 配置页面
    config_title: string,
    config_add: string,
    config_back: string,
    config_rule_name: string,
    config_rule_desc: string,
    config_rule_ext: string,
    config_rule_template: string,
    config_rule_min_size: string,
    config_rule_max_size: string,
    config_rule_enabled: string,
    config_save: string,
    config_cancel: string,
    // 导航
    nav_config: string,
    nav_main: string,
    // 主题
    theme_auto: string,
    theme_light: string,
    theme_dark: string,
}
```

## 5. 回调函数

| 回调 | 触发时机 | 预期行为 |
|------|----------|----------|
| `browse_directory()` | 点击目录浏览按钮 | 打开目录选择对话框（仅文件夹） |
| `start_work()` | 点击开始工作按钮 | 开始文件分类处理 |
| `toggle_log()` | 点击详情按钮 | 切换日志显示/隐藏 |
| `close_stats_popup()` | 点击统计弹窗关闭按钮 | 关闭统计弹窗 |
| `go_to_config()` | 点击配置按钮 | 切换到配置页面 |
| `go_to_main()` | 点击主页按钮 | 切换到主页面 |
| `add_rule()` | 点击新增规则按钮 | 打开新增规则弹窗 |
| `save_new_rule()` | 点击保存按钮 | 保存新规则到配置 |
| `cancel_add_rule()` | 点击取消按钮 | 关闭新增规则弹窗 |
| `delete_rule(int)` | 点击删除按钮 | 删除指定规则 |
| `change_theme(ThemeMode)` | 点击主题按钮 | 切换主题模式 |
| `change_language(string)` | 切换语言 | 更新界面语言 |

## 6. 属性绑定

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `working_directory` | string | "" | 工作目录路径 |
| `app_state` | AppState | Idle | 应用状态 |
| `progress` | float | 0.0 | 进度 (0.0-1.0) |
| `log_content` | string | "" | 日志内容 |
| `show_log` | bool | false | 是否显示日志 |
| `stats` | Statistics | {0,0,0,0,0} | 统计信息 |
| `show_stats_popup` | bool | false | 是否显示统计弹窗 |
| `rules` | [RuleItem] | [] | 规则列表 |
| `current_page` | PageType | Main | 当前页面 |
| `show_add_rule_popup` | bool | false | 是否显示新增规则弹窗 |
| `theme_mode` | ThemeMode | Auto | 主题模式 |
| `i18n` | I18nStrings | 中文默认值 | 国际化字符串 |

## 7. 交互流程

### 7.1 主页面工作流程

```
┌─────────┐     ┌─────────────┐     ┌─────────┐     ┌─────────┐
│  启动   │ ──▶ │  选择目录   │ ──▶ │  开始   │ ──▶ │ 处理中  │
│  应用   │     │  (浏览按钮) │     │  工作   │     │         │
└─────────┘     └─────────────┘     └─────────┘     └─────────┘
                                                         │
                     ┌───────────────────────────────────┘
                     │
                     ▼
              ┌─────────────┐     ┌─────────────┐
              │  显示统计   │ ──▶ │  关闭弹窗   │ ──▶ 回到空闲状态
              │  结果弹窗   │     │             │
              └─────────────┘     └─────────────┘
```

### 7.2 配置页面流程

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  进入   │ ──▶ │  查看规则   │ ──▶ │  新增/删除  │
│  配置   │     │  列表       │     │  规则       │
└─────────┘     └─────────────┘     └─────────────┘
     │                                     │
     │                                     ▼
     │                              ┌─────────────┐
     └────────────────────────────▶ │  返回主页   │
                                    └─────────────┘
```

## 8. 样式规范

### 8.1 间距

| 元素 | 值 |
|------|-----|
| 页面内边距 | 24px |
| 组件间距 | 20px |
| 组内元素间距 | 12px |
| 弹窗内边距 | 24px |

### 8.2 颜色方案

使用 Slint 内置的 `Palette` 系统，自动适配系统主题（明/暗模式）。

自定义颜色：
- 成功/正常：#4caf50 (Material Green 500)
- 信息：#2196f3 (Material Blue 500)
- 警告：#ff9800 (Material Orange 500)
- 错误：#f44336 (Material Red 500)
- 弹窗遮罩：#00000080 (50% 黑色)

### 8.3 字体

| 元素 | 大小 | 粗细 |
|------|------|------|
| 应用标题 | 18px | 600 (SemiBold) |
| 页面标题 | 20px | 600 (SemiBold) |
| 表格表头 | 默认 | 600 (SemiBold) |
| 弹窗标题 | 18-20px | 600 (SemiBold) |
| 其他文本 | 默认 | 400 (Regular) |

### 8.4 组件尺寸

| 组件 | 尺寸 |
|------|------|
| 导航栏高度 | 48px |
| 输入框高度 | 48px |
| 按钮高度 | 48px (主按钮) |
| 表格行高度 | 44px |
| 表格表头高度 | 40px |
| 弹窗圆角 | 12px |
| 弹窗阴影 | 20px 模糊 |

## 9. 国际化 (i18n)

支持多语言切换，默认提供中文和英文：

### 9.1 中文 (默认)

```
app_title: "🎬 MediaClassifier"
working_directory: "工作目录"
select_directory: "选择工作目录"
start_working: "开始工作"
...
```

### 9.2 English

```
app_title: "🎬 MediaClassifier"
working_directory: "Working Directory"
select_directory: "Select Directory"
start_working: "Start"
...
```

## 10. 主题支持

### 10.1 自动模式 (Auto)

跟随系统主题设置，使用 Slint 的 `Palette` 系统自动适配。

### 10.2 浅色模式 (Light)

强制使用浅色主题。

### 10.3 深色模式 (Dark)

强制使用深色主题。

## 11. 文件结构

```
crates/mc-gui/
├── Cargo.toml          # 项目配置
├── build.rs            # 构建脚本（编译 .slint 文件）
├── src/
│   └── main.rs         # 应用入口和回调实现
└── ui/
    └── main_window.slint  # 主窗口 UI 定义
```

## 12. 构建与运行

```bash
# 构建 GUI 应用
cargo build -p mc-gui

# 运行 GUI 应用
cargo run -p mc-gui

# 发布构建
cargo build -p mc-gui --release
```

## 13. 待实现功能

以下功能在当前设计阶段为占位实现，需要后续完成：

- [ ] 目录选择对话框集成（仅文件夹）
- [ ] 文件处理逻辑（调用 mc-lib）
- [ ] 实时日志输出
- [ ] 配置持久化保存
- [ ] 主题切换实际应用
- [ ] 完整的 i18n 支持
- [ ] 键盘快捷键支持
- [ ] 拖放目录支持

## 14. 依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| `slint` | 1.8 | GUI 框架运行时 |
| `slint-build` | 1.8 | .slint 文件编译 |
| `mc-lib` | workspace | 核心分类功能 |
| `anyhow` | 1.0 | 错误处理 |
| `log` | 0.4 | 日志记录 |
